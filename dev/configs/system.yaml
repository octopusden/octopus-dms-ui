jfconnect:
    enabled: false
shared:
    database:
        type: postgresql
        driver: org.postgresql.Driver
        url: "jdbc:postgresql://postgres:5432/artifactory"
        username: artifactory
        password: secret
